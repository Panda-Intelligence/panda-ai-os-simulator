# Continued extraction — 2026-09-18

User requests code/build/runtime separation to completion and explicitly reserves
GitHub repository settings and Cloudflare publishing/configuration to themselves.
No visibility/license switch, DNS, deployment, remote account mutation or social
posting. Main integrates and commits. Code truth and actual runtime tests first.

Parallel scopes: native launcher/headless standalone boundary; consumer Cloud
packaging/runner wrapper boundary. Main owns shared manifest pack/export and real
QEMU/WASM boot, state persistence, checks, source pin and PR metadata.
Retain native ABI, guest source identity and review notices; mock I/O tests do
not prove real guest startup. Existing prepared guest artifacts may be consumed
read-only in isolated tests, never copied to GitHub or treated as newest firmware.
