# Simulator Worker deployment record

Date: 2026-09-20
Owner: Codex

## Production target

- Worker: `panda-simulator`
- Custom domain: `https://simulator.pandacat.ai/`
- Version ID: `6a9fb4d4-66ce-405d-b466-0cfba3bfc11d`
- Static assets: `dist/`
- Wrangler configuration: `deploy/wrangler.jsonc`

## Verification

- `bun run test` — 55 passed
- `node --test tests/deploy-preview.test.mjs` — 2 passed
- `bun run build` — passed
- `bun run worker:dry-run` — passed
- `bun run audit:export` — passed with no findings
- `GET /` — HTTP 200 with COOP, COEP, CORP, and `nosniff`
- `GET /simulator-runtime/browser-simulator-worker.js` — HTTP 200
- `POST /` — HTTP 405 with the same isolation headers

The source repository's MIT license covers Panda-owned simulator code only.
QEMU/Espressif sources, dependencies, guest firmware, ROMs, fonts, media, and
other third-party materials retain their own notices and distribution terms.
The deployment does not imply that those separate rights or the remaining
runtime qualification gates are complete.

## Bun and English-first UI update — 2026-09-20

- Package manager: Bun 1.3.11 with frozen `bun.lock`; `package-lock.json` was removed.
- CI, Tauri launch commands, simulator scripts, and current developer docs use Bun.
- English is the default locale when no saved preference exists.
- The Language switch is in the title bar's top-right action group; saved locale preferences and Chinese options remain available.
- `bun run test` — 96 passed
- `bun run build` — passed
- `bun run worker:dry-run` — passed
- `bun run audit:export` — passed with no findings
